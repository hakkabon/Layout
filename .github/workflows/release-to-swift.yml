name: Build and Ship to Swift Wrapper

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-and-deploy:
    runs-on: macos-latest
    permissions:
      contents: write
    env:
      SWIFT_REPO: "hakkabon/Swift-Layout"
      GH_TOKEN: ${{ secrets.SWIFT_REPO_ACCESS_TOKEN }}
    steps:
      - name: Checkout Rust Code
        uses: actions/checkout@v4

      - name: Setup Rust Tools
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-ios, aarch64-apple-ios-sim, x86_64-apple-ios, aarch64-apple-darwin, x86_64-apple-darwin

      - name: Setup Rust MacCatalyst Toolchain
        run: |
          rustup toolchain install nightly
          rustup +nightly component add rust-src
          rustup +nightly target add aarch64-apple-ios-macabi x86_64-apple-ios-macabi

      - name: Run Test Suite
        run: |
          # Gate the release on the test suite actually passing. This runs
          # before any build/packaging step so a broken build (or a broken
          # test suite that silently stopped compiling) fails the workflow
          # instead of shipping a release and auto-syncing it to the Swift
          # wrapper repo.
          cargo test --all-features

      - name: Compile XCFramework and Bindings via Makefile
        run: |
          # Runs clean, macos, ios, bindings, and xcframework targets
          make clean macos ios bindings xcframework

      - name: Zip Generated XCFramework
        run: |
          cd RustLayout/Sources
          zip -r ../../Layout.xcframework.zip Layout.xcframework
          cd ../..

      - name: Extract Generated Swift Bindings
        run: |
          # Debug: Check if bindings directory exists and what it contains
          if [ -d "RustLayout/bindings/swift" ]; then
            echo "Contents of RustLayout/bindings/swift:"
            ls -la RustLayout/bindings/swift/
          else
            echo "ERROR: RustLayout/bindings/swift directory does not exist!"
            exit 1
          fi
          
          # Check if there are any .swift files
          if ! ls RustLayout/bindings/swift/*.swift 1> /dev/null 2>&1; then
            echo "ERROR: No .swift files found in RustLayout/bindings/swift/"
            exit 1
          fi
          
          # Keep track of the generated Swift file name so we can commit it to the Swift repo
          mkdir -p staging_swift
          cp RustLayout/bindings/swift/*.swift staging_swift/
          
          echo "Copied Swift files:"
          ls -la staging_swift/

      - name: Create Release Artifact in Rust Layout Repo
        uses: softprops/action-gh-release@v2
        with:
          files: Layout.xcframework.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Auto-Update Swift-Layout Repository
        run: |
          # 1. Capture the raw git tag from Rust (e.g., v0.0.1)
          RUST_TAG=${GITHUB_REF#refs/tags/}
          
          # 2. Strip the leading 'v' to make it a valid Swift SemVer string (e.g., 0.0.1)
          SWIFT_VERSION=${RUST_TAG#v}
          
          # 3. Construct the release download URL
          DOWNLOAD_URL="https://github.com/hakkabon/Layout/releases/download/${RUST_TAG}/Layout.xcframework.zip"
          
          # 4. Compute the cryptographic SHA256 checksum needed by Swift PM
          CHECKSUM=$(swift package compute-checksum Layout.xcframework.zip)
          
          # 5. Clone the destination Swift wrapper repository
          git clone https://x-access-token:${{ secrets.SWIFT_REPO_ACCESS_TOKEN }}@github.com/${{ env.SWIFT_REPO }}.git swift-repo-dir
          
          # 6. Copy the freshly generated UniFFI Swift binding source file into the Swift repo
          cp staging_swift/*.swift swift-repo-dir/Sources/SwiftLayout/
          
          cd swift-repo-dir
          
          # 7. Rewrite the target URL and Checksum with the full repository release path
          sed -i '' "s@url: \".*\"@url: \"${DOWNLOAD_URL}\"@g" Package.swift
          sed -i '' "s@checksum: \".*\"@checksum: \"${CHECKSUM}\"@g" Package.swift
          
          # 8. Commit changes and tag the Swift repository using SWIFT_VERSION
          git config --global user.name "Hakkabon Rust CI"
          git config --global user.email "ci@hakkabon.dev"
          git add Package.swift Sources/SwiftLayout/
          git commit -m "chore: automated sync for native version ${SWIFT_VERSION}"
          
          git tag ${SWIFT_VERSION}
          git push origin main --tags
